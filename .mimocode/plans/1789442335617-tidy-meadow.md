# Add `locations` to docs (tables list + project structure)

## AGENTS.md
- Line ~69: add `locations` to existing tables list

## README.md
- Project structure: add `locations/` under `(admin)/` and `components/`, add `app/api/locations/`

No other changes needed — the search fix was a bugfix, not a new pattern.
