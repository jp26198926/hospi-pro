# Plan: Add UOM column to Receiving Items DataTable

**Goal:** `/receivings/[id]` items table shows **UOM** for each product.

**Status:** **`GET /api/receiving-items` already returns `uomName`** (join `products.uomId` → `uoms.name` for print). Work is mostly **UI**.

---

## Step 1 — API (verify / small polish)

**File:** `app/api/receiving-items/route.ts`

- Confirm list GET includes `uomName` (already present)
- Optional: add same fields on **`GET /api/receiving-items/[id]`** if used elsewhere
- No schema change

---

## Step 2 — UI columns

**File:** `components/receiving-items/receiving-items-columns.tsx`

1. Interface `ReceivingItem`:
   ```ts
   uomId?: number | null;
   uomName?: string | null;
   ```
2. New column after **Product Name** (before Qty):

| Header | Cell |
|--------|------|
| **UOM** | `row.original.uomName \|\| "-"` |

Column order:
`# | Batch No. | Product Code | Product Name | UOM | Qty | Cost | Total | Expiry | Remarks | Status | Action`

No form change required (UOM comes from product); optional: show UOM in item form near Qty (product lookup) — **not required**.

---

## Step 3 — Docs (optional one-liner)

AGENTS receivings print/items: DataTable now includes **UOM** column (same `uomName` as print).

---

## Files

1. `components/receiving-items/receiving-items-columns.tsx` — interface + column
2. `app/api/receiving-items/route.ts` — only if GET-by-id needs `uomName`
3. `AGENTS.md` — optional note

---

## Verification

1. Open `/receivings/[id]` with items
2. **UOM** column shows product UOM (e.g. PCS, KG)
3. API response has `uomName` for each row
4. Print already used UOM — unchanged
5. `npx tsc --noEmit` clean
