# Update AGENTS.md, README.md, MODULE_CREATION.md for recent features

## What changed since docs were last written

1. **RBAC enforcement** — `lib/permissions.ts` (cached), `lib/api-auth.ts` (`requirePermission`, `requireAuth`, `requirePageRead`), `lib/api-client.ts` (`apiFetch`), accessToken cookie, sidebar `mine=1`, cache invalidation
2. **Currencies** — `currencies` table, `db:seed-currencies` script, `/api/currencies` (auth-only GET), `currencyId` on `settings_app`
3. **Categories module** — `categories` table with `name`, `type` (enum inventoriable/consumable), `description`
4. **Timezone-aware dates** — `lib/datetime.ts` (pure), `getAppTimezone()` in `lib/settings.ts`, all timestamps use `timestamp({ withTimezone: true, mode: "date" })`
5. **Sidebar** — conditional `overflow-y-auto` (only when expanded), `sidebar-scroll` class

## Updates per file

### AGENTS.md

- **Commands**: add `npm run db:seed-currencies`
- **Architecture**: add bullet for RBAC enforcement (lib/permissions.ts, lib/api-auth.ts, how requirePermission works)
- **Architecture**: add `lib/datetime.ts` (pure formatting, no db imports)
- **Auth bullet**: mention accessToken cookie (in addition to localStorage) for SSR/page guards
- **Schema conventions**: update timestamp examples to `timestamp("...", { withTimezone: true, mode: "date" })`
- **Existing tables list**: add `categories`, `currencies`
- **CRUD module pattern**: add note that API routes must call `requirePermission` and pages must call `requirePageRead`
- **API conventions**: add permission mapping table (GET→Read, POST→Add, PUT→Edit, DELETE→Delete, PATCH→Restore)
- **Gotchas**: add `lib/datetime.ts` must stay pure; parent pages need View grant for sidebar; accessToken cookie must be cleared on logout

### README.md

- **Features**: add "Full RBAC enforcement (sidebar filtering, page guards, API permissions)", "Categories management", "Multi-currency support", "Timezone-aware date display"
- **Available Scripts**: add `npm run db:seed-currencies`
- **Project Structure**: add `categories/`, `currencies/`, `lib/api-auth.ts`, `lib/api-client.ts`, `lib/datetime.ts`, `lib/permissions.ts`, `scripts/seed-currencies.ts`
- **Architecture**: add RBAC section explaining the permission model
- **Schema examples**: update `timestamp()` → `timestamp("...", { withTimezone: true, mode: "date" })`
- **How to Add a New Module**: replace the long inline guide with a reference to `MODULE_CREATION.md` (it's the canonical guide now)

### MODULE_CREATION.md

- **Step 1 schema examples**: update all `timestamp()` to `timestamp("...", { withTimezone: true, mode: "date" })`
- **Step 3 API List+Create**: add `requirePermission` calls (GET→Read, POST→Add) with code example
- **Step 4 API Single**: add `requirePermission` (GET→Read, PUT→Edit, DELETE→Delete, PATCH→Restore)
- **Step 6 Pages**: add `requirePageRead("<path>")` at top of page component
- **New step or note**: grant View+Read on parent page for sidebar visibility
- **New note**: pass timezone prop from `getAppTimezone()` to table components for date display
- **Checklist**: add permission checks + page guard items
- **Gotchas**: add datetime purity, parent View grant, accessToken cookie

## Verification

- Read each file after editing — confirm no stale `timestamp(` without `withTimezone`
- Confirm all new lib files are mentioned
- Confirm MODULE_CREATION.md checklist includes permission steps
