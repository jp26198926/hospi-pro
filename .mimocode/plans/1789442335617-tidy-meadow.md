# GST Type Module — Full CRUD

Fields: `code` (unique, required), `name` (unique, required), `status` (commonStatusEnum, default Active). Audit fields included.

## Files

### 1. Schema — `lib/db/schema.ts` (append after `products`)

```ts
export const gstTypes = pgTable("gst_types", {
  id: serial("id").primaryKey(),
  code: text("code").notNull().unique(),
  name: text("name").notNull().unique(),
  status: commonStatusEnum("status").notNull().default("Active"),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).defaultNow().notNull(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }),
  deletedAt: timestamp("deleted_at", { withTimezone: true, mode: "date" }),
  createdBy: integer("created_by").references((): AnyPgColumn => users.id),
  updatedBy: integer("updated_by").references((): AnyPgColumn => users.id),
  deletedBy: integer("deleted_by").references((): AnyPgColumn => users.id),
  deletedReason: text("deleted_reason"),
});
```

Then `npm run db:push`.

### 2. Validation — `lib/validations/gst-type.ts`

### 3. API — `app/api/gst-types/route.ts` + `[id]/route.ts`

Standard CRUD with `requirePermission(request, "/gst-types", ...)`. Uniqueness on both code and name. Audit fields from `auth.userId`.

### 4. Components — `components/gst-types/` (5 files)

Columns: `code`, `name`, `status`, `createdAt`, `updatedAt`, `actions`.

### 5. Pages — `app/(admin)/gst-types/` (3 files)

### 6. Sidebar + grants

Insert under Data (id=8): `('GST Type', '/gst-types', 'file-text', 9, 'Active')`. Grant Administrator all permissions.

### 7. Docs

Add `gstTypes` to AGENTS.md tables list, README project structure.

## Verification

1. `npm run db:push` succeeds
2. `npx tsc --noEmit` passes
3. Full CRUD works with audit fields
4. Sidebar shows GST Type under Data
