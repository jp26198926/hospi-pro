# Doc sync after products work

## Answer

**Yes — partial updates needed.** Code is ahead of docs in a few places. Created At rule, SearchableSelect, and bigint conventions are already correct. `proxy.ts` / `CLAUDE.md` need no changes.

Scope: **Must + high-value nice** (approved).

## Already correct (do not touch)

- AGENTS.md **Columns** Created At rule (products as wide-table reference)
- MODULE_CREATION.md Created At rule (Step 5, checklist, Gotchas)
- README bigint/FK wording in How to Add a New Module
- CLAUDE.md `@AGENTS.md`
- `/api/products/next-code` is protected (not in `PUBLIC_API_ROUTES`)

---

## 1. README.md

**Features — Products bullet (~L16)**

From:
`Products management (editable product codes, category/GST type FKs, stock/cost tracking)`

To:
`Products management (editable product codes with optional next-code prefill, required UOM + GST type FKs, optional category, stock/cost tracking)`

---

## 2. AGENTS.md

(Do **not** edit the auto `<!-- BEGIN/END:nextjs-agent-rules -->` block.)

| Location | Change |
|----------|--------|
| **~L100** CRUD pattern | Fix stale pointer: “Available icon keys are listed in `README.md`.” → “Available icon keys are listed in `MODULE_CREATION.md` (Step 7) and mapped in `components/layout/sidebar.tsx`.” |
| **~L87** CRUD pattern | After “Follow the Departments/Roles module…”, add one line: products is the reference for required FKs (`uomId`, `gstTypeId`), join display names, editable unique codes, and Created At omission. |
| **API conventions** | Note: helper routes under a module (e.g. `GET /api/products/next-code`) use `requirePermission` with the **parent** pagePath (e.g. `/products`) — do **not** add them to `PUBLIC_API_ROUTES`. |
| **Gotchas** | Add: **FK form lookups** — form/search modals fetch list APIs (e.g. `/api/uoms`, `/api/categories`, `/api/gst-types` for products). Roles that can open the parent module still need **Read** on those FK pages or dropdowns render empty. |

---

## 3. MODULE_CREATION.md

| Location | Change |
|----------|--------|
| **Header ~L5** reference modules | Add `products` as the wide / FK-rich / editable-code reference (alongside departments, roles). |
| **Step 3 / Gotchas** | **User-editable unique codes**: if the domain has a code (products, UOM), zod `trim().min(1).max(50)` (or module max), POST uniqueness `eq(code)` + `ne(status,"Deleted")`, PUT uniqueness also `ne(id, self)`. Optional prefill endpoint e.g. `GET /api/<plural>/next-code` with parent Read permission — never public. |
| **Step 5 / Gotchas** | **Required FK recipe** (short): schema `bigint("col_id",{mode:"number"}).notNull().references(...)`; zod `z.number().int().positive("X is required")`; form `SearchableSelect` (no `allOption`, empty → `0`); list/detail `leftJoin` + `*Name` in select/exports/detail. |
| **Checklist** | Add items: (a) required FK column + join `*Name` if any; (b) unique code checks if applicable; (c) helper/prefill API uses parent `requirePermission`, not `PUBLIC_API_ROUTES`; (d) grant role Read on FK lookup pages used by forms. |

---

## Files to edit

- `README.md`
- `AGENTS.md` (outside auto-regen block)
- `MODULE_CREATION.md`

No application code.

---

## Verification

1. Re-read the three files — no remaining “icon keys in README”, products bullet mentions UOM + editable codes.
2. AGENTS Gotchas include FK Read + helper-route public-API notes.
3. MODULE_CREATION references products; checklist/gotchas cover unique codes, required FKs, next-code-style endpoints, FK page Read grants.
4. Created At / SearchableSelect / bigint text unchanged and still accurate.
