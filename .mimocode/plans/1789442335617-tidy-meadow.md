# Plan: View Adjustment modal — 2-column layout

**File:** `components/adjustments/adjustment-view-cancel-modals.tsx` (`AdjustmentViewModal` body)

**Layout**
- Wrap field list in **`grid grid-cols-1 gap-3 sm:grid-cols-2`**
- **Full width** (`sm:col-span-2`): **Product**, **Remarks**
- **Two-column pairs** (left/right):
  - Date | UOM
  - Location | Status
  - Old Qty | Adj Qty
  - New Qty | Created By  
  (or: Date|Status, Location|UOM, Old|Adj, New|Created By — same result)
- Each field card keeps existing `rounded-sm border-[#eee] bg-[#fafafa] p-3` style
- Status badge stays in its cell

**Optional:** same treatment only in View modal (Cancel modal body unchanged).

## Verification
1. `/adjustments` → View → fields in two columns; Product & Remarks span full width
2. Mobile stacks to one column (`grid-cols-1` default)
3. `npx tsc --noEmit` clean
