# Plan: Sync README / AGENTS / MODULE_CREATION with recent code

**Answer: yes** — three gaps after receivings + stock user display + date conventions.

---

## What is already current

| Topic | Where |
|-------|--------|
| Receivings feature + stock posting | README L19; AGENTS L83; MODULE_CREATION reference L5 |
| Dates `YYYY-MM-DD` + DatePicker | README L24; AGENTS Dates; MODULE_CREATION detail/checklist/gotchas |
| `stock_levels` / `stock_movements` special tables | AGENTS L85–87 |
| `receiving_status` Draft/Completed/Cancelled + `deleted_*` audit | AGENTS L83 |
| Receivings UI `formatUserDisplay` note | AGENTS L83 only (partial) |

---

## Gaps to fix

### 1. README.md

| Change | Detail |
|--------|--------|
| Features | Add bullet: **User display** — Created/Updated/Deleted By shown as `[lastname], [F].` (`lib/format-user.ts`) on inventory/receivings screens |
| Receivings bullet | Optional: note master-detail items + Print when Completed |
| Audit fields bullet | Clarify standard `deletedAt/deletedBy/deletedReason` on soft-delete **and** receivings (status enum separate) |
| File tree (if `datetime.ts` line exists) | Mention `format-user.ts # formatUserDisplay → Doe, J.` |

### 2. AGENTS.md (outside BEGIN/END)

| Section | Add |
|---------|-----|
| **UI conventions** | New **User names** bullet: use `formatUserDisplay(firstname, lastname)` from `@/lib/format-user` for **Created By / Updated By / Deleted By** columns and detail cards. Format `[lastname], [F].`. APIs map `*Display` fields (e.g. `createdByDisplay`); email optional fallback. Applied on receivings, stock-levels, stock-movements. New transaction modules must follow this. |
| **Schema / receivings** | Already good; optional note that stock APIs also return `updatedByDisplay` / `createdByDisplay` |
| **API conventions** | Note: document modules may filter status as `Draft`/`Completed`/`Cancelled` instead of Active/Deleted |
| **CRUD pattern** | Optional pointer: master-detail documents follow **receivings** (roles for layout; items child table; complete/cancel workflows) |

### 3. MODULE_CREATION.md

| Section | Add |
|---------|-----|
| Checklist | After date/display lines: user columns use **`formatUserDisplay`** + join/select firstname/lastname |
| Gotchas | **User display**: `[lastname], [F].` via `lib/format-user.ts` — not email, not full name |
| Gotchas | **Document workflows**: new enum allowed when status is not Active/Deleted (e.g. receivings); audit columns still `deleted_*` |
| Gotchas | **Master-detail**: copy receivings for items-on-detail; stock posting via `db.transaction` |
| Status / API text | Soft-delete modules still Active/Deleted; **exception** document status enums |
| Reference modules | Already lists receivings — keep |

---

## Files to edit

1. `README.md`
2. `AGENTS.md` — UI User names + any small API/schema notes
3. `MODULE_CREATION.md` — checklist + gotchas for formatUserDisplay, document enums, master-detail

No code changes.

---

## Verification

1. Docs-only review: no tool/typecheck required
2. Grep docs for `formatUserDisplay`, `YYYY-MM-DD`, `receiving_status`, `deleted_at`, `receivings`
3. AGENTS auto `<!-- BEGIN/END -->` **untouched**
4. New module guidance matches: dates = formatDateOnly/DatePicker; users = formatUserDisplay; receivings = workflow + stock post reference

---

## Out of scope

- Migrating all other modules (roles, products, etc.) to `formatUserDisplay` — only inventory/receivings unless requested
- Changing receivings/stock code
